import { GenresShowPipe } from './genres-show.pipe';

describe('GenresShowPipe', () => {
  it('create an instance', () => {
    const pipe = new GenresShowPipe();
    expect(pipe).toBeTruthy();
  });
});
